# Scripting, Parsing, and Automation

Last audited: 2026-08-30.
Targeted source check: 2026-09-09 (persisted singleton trigger-mode compatibility).

This area covers CBS/template parsing, ChatML and rendered-message parsing, regex scripts, slash commands, trigger effects, Lua/Python execution, owner-scoped automation writes, and script-definition editing. Prompt assembly uses many of these features and is cross-referenced in [Prompting, Generation, and Streaming](prompting-generation-and-streaming.md).

## Test groups

| Logical group                                      | Relevant locations and included cases                                                                                                                                                                                                                              | Behavior and importance                                                                                                                                                                                                                                                                                                                                                         | Effectiveness and overall value                                                                                                                                                                                                                                                                                                                                                               |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CBS conditionals, loops, strings, and escapes      | `src/ts/parser/tests/cbs/{conditionals,eachReinjection,escapes,history,loop,strings,phase9BaselineDrifts.dom}.test.ts` plus `test/compat-harness/phase9CbsBaseline.test.ts` | Cases cover `#if`, `#if_pure`, `#when`, comparisons/logical operators/right-to-left evaluation, variable/toggle operators, else/nesting, `#each` literals/variables/nesting/reinjection and element/output caps, escape tokens, `#pure`, string transforms, default variables, Unicode, matcher aliases, recursion limits, and malformed `firstmsgindex` literal preservation. History cases distinguish the greeting-inclusive unbounded form from chronological N-message windows, optional role prefixes, oversize windows, and invalid counts. | Strong pure regression coverage. Exact strings and property-based escape inputs protect realistic prompt/template behavior. The formerly weak diagnostics are corrected: colon asserts both supported colon aliases, omitted-`as` covers literal and variable arrays, and literal/variable 2-D loops have distinct titles. Server CBS recursion now carries the call-stack budget through nested field expansion. |
| Chat/message parsing and partial edits             | `src/ts/parser/partialEdit.test.ts`; `sentenceBreaks.test.ts`; `tests/{chatML,chatVar.svelte,inlayBlobCache,renderFastPaths}.test.ts`; Chat/ChatBody parser-dependency and custom-HTML UI tests | Verifies normalized partial-edit mapping and fallback cap; sentence paragraph grouping across Latin/CJK/Korean, fenced/structural/protected spans, bilingual wrappers, quotations, and missing `Intl.Segmenter`; ChatML roles/thoughts; chat variables; cached asset reads; and parser fast paths. | Good output and resource-lifecycle coverage. Sentence splitting has strong exact preservation oracles. Partial-edit remains narrow, and broader markdown/custom-HTML behavior relies heavily on component mocks. |
| Regex scripts and display transformations          | `src/ts/process/{scripts.editdisplay,scripts.importRegex,scripts.regexCache}.test.ts`, server `boundedRegex.test.ts` and `scripts.test.ts`, and SideBars script editor tests                                                                                     | Covers edit-display injection, message patch/rollback, active-preset ownership, malformed rows, import cancellation/validation/legacy normalization, compiled-regex and best-match LRU caches, directives, moves/repeats, DSL actions, output preparation, prepared-script memoization, overlapping-alternative complexity rejection, the configurable 16 MiB default replacement/output limit, and bounded replacement/split/move output. Server replacement CBS receives the actual current-message index, including the newly appended input row.                                       | High value. Exact transformed text/state and no-write/no-log assertions catch user-visible regressions. Cache instance/compile-count checks are intentionally implementation-coupled. Import/editor tests do not prove that every authored combination executes correctly after persistence/reload.                                                                                       |
| Slash commands and guarded durable effects         | `src/ts/process/__tests__/command.owner.test.ts` and chat/file command tests                                                                                                                                                                          | Covers `/send`, `/sendas`, `/comment`, `/cut` by range/index/id, `/del`, `/multisend`, `/setvar`, `/addvar`, pipe returns, active-chat changes, scoped failure rollback, absence of whole-database writes/clones, and warm-path logging.                                                                                                                                        | Critical. These cases assert command payloads and resulting transcript/scriptstate through explicit resource owners. They are stronger than tests that merely spy on command invocation. Visible DOM results and browser reload are not covered here.                                                                                                                     |
| Lorebook resource guards and stable import targets | `src/ts/process/__tests__/lorebook.owner.test.ts`                                                                                                                                                                                                      | Refuses to mutate, import, or export unhydrated lorebook stubs; dispatches global add/folder/import operations through commands; captures the original character/chat/global owner across asynchronous file selection; and rolls back only attempted fields while preserving concurrent edits.                                                                                  | Critical data-loss coverage. Exact no-write, target, payload, and rollback assertions protect a particularly dangerous stale-stub path. File picking/transport and the rendered lorebook editor remain mocked, so one mounted/import-reload journey is still needed.                                                                                                                      |
| Trigger routing, control flow, and budgets         | Client `triggers.{clientBudget,cloneCost,regexMemo}.test.ts` plus `__tests__/triggers.owner.test.ts`; server `triggers.test.ts` and the production-shaped module case in `displaySources.test.ts` | Includes source/module matching, scalar-only automatic modes, finite persisted singleton-tuple compatibility without rewrite, preserved manual/Lua/code bypasses, variables and conditions, V1/V2 effects, loop/wait/abort/resend budgets, retained whole-trigger guards for malformed container names and out-of-mode display/request effects, cache and bounded regex, data operations, display/request state, character/persona/lorebook/author-note/scriptstate durable writes, stale targets, and narrow rollback. | Critical but uneven. The real persisted loader/endpoint case proves ten tuple triggers remain skipped while a canonical trigger runs. Control-flow, budget, abort, durable-write, and representative data effects are strong. Many string, array, dictionary, random, and message-reader arms still lack direct cases. Clone/compile counters overlap behavioral cases but guard performance. |
| Lua runtime and host authority                     | `src/ts/process/scriptings.test.ts`, server `luaRuntime.test.ts`, and Lua cases in generation/trigger tests | Covers client edit-display fallback, time-bounded infinite loops, engine/code-hash LRU reuse, current-run host bindings, access-key cleanup, scoped Fastify chat/scriptstate writes, stale targets, description type validation, server network SSRF/rate/abort restrictions, low-level LLM/lorebook APIs, aggregate execution limits, engine pooling, and interactive failure. | Security-critical and good. Server cases run the Lua engine and assert network/state effects; client cache tests validate cleanup and current-run binding. Continue targeting untested host and failure paths. |
| Python worker protocol                             | Python group in `src/ts/process/scriptings.test.ts`                                                                                                                                                                                                                | Six cases cover init plus a structured call, synchronous host APIs during a request, host failure propagation, cached-context termination, initialization deadline/recreation, and call deadline/access-key cleanup.                                                                                                                                                                                                                 | Useful but narrow. The mocked protocol and deadline/recovery basics are asserted, but there is no real Pyodide/browser-worker execution, abort matrix, large transfer, concurrent contexts, or browser cleanup/restart journey.                                                                                                                                                                                 |
| Definition owners and editing UI                   | `src/ts/server/{scriptDefinitionOwner.svelte,scriptDefinitionMutations}.test.ts`; `src/lib/SideBars/Scripts/{DefinitionDeleteRace,RegexData,RegexList,TriggerV1Data,TriggerV2List}.svelte.test.ts`; `triggerV2Import.test.ts`; trigger language-description tests | Covers compact create/update/delete/reorder classification, character/module owner drafts, the global settings draft watcher, scoped rollback/flush, delete races, import validation, editor controls, and localized descriptions.                                                                                                                                             | Strong state, mixed UI depth. Owner suites assert narrow rollback and concurrent edits well. Some component cases focus on callbacks/element existence and do not run the saved definition through the production engine.                                                                                                                                                                 |

## Especially critical tests

- CBS nested/recursive/budget cases prevent malformed or hostile templates from hanging generation.
- Owner-scoped command and trigger suites protect the Fastify-only architecture from accidental aggregate database writes and broad rollback.
- Server Lua SSRF, execution-budget, abort, and low-level-call cases protect a powerful extension boundary.
- Trigger client/server budget and regex-cap tests prevent runaway automation on chat send or render.
- Definition delete-race and stale-owner cases prevent one editor from deleting or restoring a newer definition.

## Primary inventory

| Location                  | Included test files / groups                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Parsers                   | `src/ts/parser/partialEdit.test.ts`; `sentenceBreaks.test.ts`; `src/ts/parser/tests/chatML.test.ts`, `chatVar.svelte-node.test.ts`, `inlayBlobCache.test.ts`, `renderFastPaths.test.ts`; CBS `conditionals`, `eachReinjection`, `escapes`, `history`, `loop`, `strings`, `phase9BaselineDrifts.dom`, and `test/compat-harness/phase9CbsBaseline.test.ts`. |
| Client automation runtime | `src/ts/process/scriptings.test.ts`, `scripts.editdisplay.test.ts`, `scripts.importRegex.test.ts`, `scripts.regexCache.test.ts`, `triggers.clientBudget.test.ts`, `triggers.cloneCost.test.ts`, `triggers.regexMemo.test.ts`, `__tests__/command.owner.test.ts`, `lorebook.owner.test.ts`, and `triggers.owner.test.ts`. |
| Server runtime            | `server/fastify/__tests__/boundedRegex.test.ts`, `luaRuntime.test.ts`, `promptVariables.test.ts`, `scripts.test.ts`, and `triggers.test.ts`; scripting groups inside `assemble.test.ts` and `generation.chat.test.ts`.                                                                                                                                               |
| Durable definition state  | `src/ts/server/scriptDefinitionOwner.svelte.test.ts`, `scriptDefinitionMutations.test.ts`, and definition command groups in browser/server command suites. These are primarily inventoried in [Domain Mutations and Editing Owners](domain-mutations-and-editing-bridges.md).                                                                                                      |
| UI                        | `src/lib/SideBars/Scripts/DefinitionDeleteRace.svelte.test.ts`, `RegexData.svelte.test.ts`, `RegexList.svelte.test.ts`, `TriggerList.svelte.test.ts`, `TriggerV1Data.svelte.test.ts`, `TriggerV2List.svelte.test.ts`, `triggerV2Import.test.ts`, plus language trigger-description tests.                                                                                                  |
