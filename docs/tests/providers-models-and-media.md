# Providers, Models, and Media

Last audited: 2026-08-02.

This area covers durable model profiles and role bindings, provider capability/routing, provider-specific wire adapters, secret ownership, fixed server operations, translation, image generation, transcription, TTS, and browser media processing. Prompt orchestration is assessed in [Prompting, Generation, and Streaming](prompting-generation-and-streaming.md); MCP OAuth is assessed in [Plugins, Modules, and MCP](plugins-modules-and-mcp.md).

## Test groups

| Logical group | Relevant locations and included cases | Behavior and importance |
| --- | --- | --- |
| Model-profile and split-preset records, roles, and resolution | `src/ts/model/{modelProfileRecords,modelProfileResolver,modelProfileUiState,modelRoles,modelPresetSnapshots}.test.ts`, `modelProfileMutations.test.ts`, `src/ts/presetSplit.test.ts`, `presetFieldMirror.test.ts`, `server/fastify/__tests__/modelProfileResolver.server.test.ts`, model-profile command groups, and Settings Model component tests | Covers normalization/schema, durable/legacy/inherit role bindings, profile/runtime/default precedence including inherited and explicit-false `stripCoT`, fallbacks, static models, provider-specific option resolution, incomplete/unsupported status, legacy UI visibility and mutations; split model/prompt extraction, dedupe, role ownership, export/composition/aliases/cloning; and delayed ID-bound top-level mirroring without generic `promptTemplate` leakage. |
| Dynamic catalog credential isolation | `src/ts/model/modellist.dynamic.test.ts` | Registers dynamic Google and Anthropic models while ensuring catalog credentials are not exposed to a cross-origin consumer. |
| Shared credentials, secret masking, and stable identity | `packages/shared-core/src/providerCredentialRecords.test.ts`, `ProviderCredentialList.svelte.test.ts`, `modelProfileSecrets.test.ts`, `providerSecretMask.test.ts`, browser fixed-operation adapters, server `providerSecrets.test.ts`, `chatDispatchProfileOptions.test.ts`, resource-read masking, and route tests | Covers shared-credential CRUD/reference rules, preserve/replace/clear draft semantics, nested registered secrets, stable profile/row restoration, unknown/moved identity rejection, copy/in-place parity, stored-sentinel handling, endpoint-key binding, and sanitized errors/traces. |
| Capability, tokenization, and role routing | Client `request/tests/{providerCapability,modelRoleRouting,serverPromptAssembly}.test.ts`, `src/ts/tokenizer.test.ts`, `src/ts/model/{llmgateway,neuralwatt}.test.ts`, and the shared `src/ts/model/tokenizerOptions.ts` catalog; server `providerCapabilityRoute.test.ts`, `tokenizerConfig.test.ts`, `tokenizerGoldenCounts.test.ts`, generation routes, and profile option matrices | Expanded cases classify supported provider/profile variants and primary/fallback/static/script roles. Tokenizer cases pin explicit cl100k/o200k and portable/web selection, automatic/legacy routing, golden counts, and unsupported configurations. Managed OpenAI-compatible provider cases cover catalog mapping and fixed-endpoint routing. |
| OpenAI-compatible families | Server `openai.test.ts`, `openaiResponses.test.ts`, `openaiLegacyInstruct.test.ts`, `generation.completion.test.ts`, `additionalParams.test.ts`, `providerMessages.test.ts`; client `openaiProfileOptions.test.ts`, `openaiResponsesLegacyProfileOptions.test.ts`, and additional-param tests | Covers Chat/Responses/legacy completion bodies, system/Ooba hoisting, OpenRouter/NanoGPT/custom/reverse-proxy/Ollama-cloud variants, hosted search/tools, JSON controls, additional headers/body/path parameters and prototype safety, multimodal/reasoning rows, SSE fragmentation, metrics, errors, and abort. |
| Anthropic, Bedrock, Gemini, and Vertex | Server `anthropic.test.ts`, `bedrock.test.ts`, `gemini.test.ts`, `sigv4.test.ts`, `vertexAuth.test.ts`; client `anthropicProfileOptions.test.ts` and `google.fastify.test.ts` | Covers native message/media/thinking/tool conversion, cache points, buffered/streaming UTF-8, Bedrock credential parsing and deterministic signing, Gemini thoughts/tools/metrics, Google AI Studio vs Vertex selection, JWT exchange/cache/deduplication/expiry, and abort/error bounds. |
| Other text providers | Server `cohere`, `mistral`, `ollama`, `horde`, `kobold`, `oobaLegacy`, and `echo`; client corresponding profile-option tests and `src/ts/horde/getModels.test.ts` | Covers message reformats, endpoint/model/key/sampler precedence, safety modes, native and OpenAI-compatible Ollama tools, NDJSON/SSE/WebSocket/poll lifecycles, model discovery caching, anonymous Horde, malformed frames, abort, and bounds. |
| Fixed provider and embedding operations | Browser `src/ts/server/{providerOperations,embeddingOperations}.test.ts`; server `providerOperations.test.ts`, `embeddingOperations.test.ts`; `src/ts/model/neuralwatt.test.ts`; NanoGPT/OpenRouter/Ollama catalogs and dashboard tests | Tests the allowlisted operation discriminator, typed input validation, stored/provided credential selection, endpoint binding, response/deadline/byte/vector bounds, disconnect cancellation, error sanitization, and keyed cache/dedupe/retry behavior. Neuralwatt cases map public catalog metadata/pricing/capabilities, retry malformed results, cache successful results, and dispatch through its fixed OpenAI-compatible endpoint. |
| Translation and generated-message follow-up | All tests under `src/ts/translator/`; `src/ts/process/serverGeneratedMessageTranslation.test.ts`; `src/ts/server/greetingTranslations.test.ts`; server `rawMessageTranslation.test.ts`, `serverMessageTranslation.test.ts`, `serverAutoTranslationEligibility.test.ts`, `generationChatCompletionTranslation.test.ts`, `greetingTranslationStore.test.ts`, and provider-operation translation cases; translator UI/Playground tests | Covers bilingual line/fence interleaving and sanitized styling, preset/history-slot pipelines, role/profile keyed caches, secret-free signatures, quota fallback, raw-message provider resolution, persisted multi-step presets/history, optional `<Thoughts>`/`<think>` removal from send-text-as-is source plus source/translated history, generated-message eligibility, source-fenced greeting translation/recovery, connected hold/cap/detach outcomes, notifications, async persistence, abort, and sanitized errors. |
| Request-history persistence and UI | Server `requestHistory.test.ts` and `requestHistoryRoutes.test.ts`; `src/lib/Setting/Pages/RequestHistorySettings.svelte.test.ts`; provider/generation suites that assert captured attempt metadata | Covers bounded summaries/details, completion/failure state, retention pruning, authenticated read/delete policy, duration and sanitized API metadata display, stale detail ownership, and empty/error/loading states. |
| Image generation and transcription | Browser `src/ts/process/{stableDiff}.test.ts`, `__tests__/{igp,imggenStableDiff}.test.ts`, `src/ts/server/{imageGeneration,openAITranscription}.test.ts`; server `imageGeneration.test.ts`, `openAITranscription.test.ts`; image/translation Playground cases | Covers NovelAI, DALL-E, Stability, Fal, Imagen, WaveSpeed, Kei and OpenAI-compatible shapes; reference asset fallback, stored secrets, target supersession, cancellation, response types/caps/errors, prompt routing, and fixed Whisper upload/VTT validation. |
| TTS and browser audio lifecycle | `src/ts/process/tts.test.ts`, `ttsHooks.test.ts`, `transformers.test.ts`, browser `src/ts/server/tts.test.ts`, server `tts.test.ts`, TTS asset/UI tests | Covers Web Speech absence/injection, voice-catalog cache/retry, masked and draft credentials, every server-owned provider path, PCM-to-WAV, AudioContext/source/gain reuse and cleanup, stop/supersession/late transport, hook sequencing/skip/error/timeout/abort, Hugging Face translation, VITS model disposal and abort. |
| Image codecs and file media helpers | `src/ts/media/tests/imageType.test.ts`, `compressImage/tests/{compressImage,lossyCompression}.test.ts`, `src/ts/process/dynamicutils/pdf.test.ts` (cleanup/failure cases), plus inlay/CharX tests | Covers format magic bytes, compression bypass/dispatch, browser decode/canvas failure settlement, quality value, image bounds/aspect ratio, object-URL cleanup, awaited PDF document/page/loading-task cleanup on success and each failure point, and oversized archive/media rejection. |

## Especially critical tests

- Secret masking/restoration and fixed-operation endpoint binding prevent credential disclosure and confused-deputy requests.
- Capability/routing parity prevents settings from advertising configurations that Fastify cannot execute.
- Strip CoT inheritance/dispatch cases protect the output-privacy choice without forcing it on profiles that explicitly disable it.
- Provider stream fragmentation, terminal error, byte-cap, and abort cases protect long-running chat reliability.
- SigV4/Vertex token cache and credential-isolation tests protect expensive and security-sensitive authentication flows.
- TTS/image supersession and cancellation tests prevent stale media from applying to a new character/chat target.

## Primary inventory

| Location | Included test files / groups |
| --- | --- |
| Browser model records/catalogs | Model tests under `src/ts/model/`: `llmgateway`, `neuralwatt`, `modelPresetSnapshots`, `modelProfileMutations`, `modelProfileRecords`, `modelProfileResolver`, `modelProfileSecrets`, `modelProfileUiState`, `modelRoles`, `modellist.dynamic`, `nanogpt`, `ollama`, `openrouter`, and `providerCredentialRecords`; plus `providerSecretMask.test.ts`. |
| Split preset contracts | `src/ts/presetSplit.test.ts` (extraction/composition/ownership cases) and `src/ts/presetFieldMirror.test.ts` (stable-field-mirror cases). Mutation/replay behavior is cross-indexed in [Domain Mutations and Editing Owners](domain-mutations-and-editing-bridges.md). |
| Browser request adapters | `src/ts/process/request/tests/additionalParams.test.ts`, `anthropicProfileOptions`, `cohereHordeOobaLegacyProfileOptions`, `google.fastify`, `koboldProfileOptions`, `modelRoleRouting`, `ollamaProfileOptions`, `openaiProfileOptions`, `openaiResponsesLegacyProfileOptions`, and provider-capability/completion cases cross-indexed with prompt generation. |
| Catalog, translation, and media runtime | `src/ts/horde/getModels.test.ts`; translator `bilingualInterleave.dom.test.ts`, `bilingualInterleave.test.ts`, `pipeline.test.ts`, `presets.test.ts`, `translator.cache.svelte-node.test.ts`, and `translator.html.test.ts`; `src/ts/process/serverGeneratedMessageTranslation.test.ts`; `src/ts/server/greetingTranslations.test.ts`; all `src/ts/media/**/*.test.ts` files; `src/ts/process/dynamicutils/pdf.test.ts`, `stableDiff.test.ts`, `transformers.test.ts`, `tts.test.ts`, and `ttsHooks.test.ts`. |
| Browser fixed operations | `src/ts/server/providerOperations.test.ts`, `embeddingOperations.test.ts`, `imageGeneration.test.ts`, `openAITranscription.test.ts`, `tts.test.ts`, `nanoGPTDashboardFetch.test.ts`, and settings-media upload tests. |
| Server providers/media | Provider/profile suites: `additionalParams`, `anthropic`, `bedrock`, `chatDispatchProfileOptions`, `cohere`, `echo`, `embeddingOperations`, `gemini`, `generation.completion`, `greetingTranslationStore`, `horde`, `imageGeneration`, `kobold`, `mcpOAuthRefresh`, `mistral`, `modelProfileResolver.server`, `ollama`, `oobaLegacy`, `openAITranscription`, `openai`, `openaiLegacyInstruct`, `openaiResponses`, `providerCapabilityRoute`, `providerMessages`, `providerOperations`, `providerSecrets`, `providerTransport`, `requestHistory`, `requestHistoryRoutes`, `sigv4`, `staleInlineModelProfileSecrets`, `tokenizerConfig`, `tokenizerGoldenCounts`, `tts`, and `vertexAuth`; translation follow-up suites named above. MCP OAuth is analyzed primarily in the MCP document. |
| UI | Model settings/list/editor/credential/runtime/fallback tests under `src/lib/Setting/Pages/Model/`; Request History settings; provider list/actions, NanoGPT/OpenRouter model UI tests; Playground translation/image/tool tests. Detailed interaction/accessibility quality is assessed in the UI documents. |
